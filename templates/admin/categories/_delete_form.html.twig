<form method="post" action="{{ path('admin_categories_delete', {'id': category.id}) }}" onsubmit="return confirm('Voulez vous supprimer la catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="p-2 rounded-xl bg-red-500 text-white hover:bg-red-800">Supprimer</button>
</form>
