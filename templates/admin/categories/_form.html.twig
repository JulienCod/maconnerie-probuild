{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="p-2 rounded-xl mt-4 bg-blue-500 text-white hover:bg-blue-800">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
