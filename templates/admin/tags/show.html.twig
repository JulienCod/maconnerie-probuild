{% extends 'admin/adminBase.html.twig' %}

{% block title %}Tags{% endblock %}

{% block body %}
    <h1>Tags</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>nom</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_tags_index') }}">Retour à la liste des tags</a>

    <a href="{{ path('admin_tags_edit', {'id': tag.id}) }}">Modifier le tag</a>

    {{ include('admin/tags/_delete_form.html.twig') }}
{% endblock %}
